# Scorekeeper

Static scorekeeping site ready for GitHub Pages.

## Local preview

Open `index.html` directly in a browser.

## GitHub Pages

This repo includes a GitHub Actions workflow at `.github/workflows/deploy.yml` that deploys the repository root as a static site.
